##Updating readme to include Terminal commands used during GPS session.

+ cd
+ ls
+ git branch
+ git checkout -b
+ git remote -v
+ mkdir
+ git add
+ git status
+ git push
+ git commit -m
+ subl .
+ git clone
